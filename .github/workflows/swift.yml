name: Swift

on:
  push

jobs:
  build:

    runs-on: ios

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: swift package clean; swift build -v
    - name: Run tests
      run: set -o pipefail && swift test -v 2>&1 | xcbeautify --renderer github-actions